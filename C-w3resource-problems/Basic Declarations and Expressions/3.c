#include <stdio.h>
#include <stdlib.h>

// C itself doesn't have a multi-string method like python or JS has 
// so we have to do this. but generally you could read each line from a txt file and print them

int main (void) {
    puts("######");
    puts("#");
    puts("#");
    puts("#####");
    puts("#");
    puts("#");
    puts("#");
    return EXIT_SUCCESS;
}
